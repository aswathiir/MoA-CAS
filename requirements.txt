# IndicConformer Benchmark — dependencies
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0      # AutoModel + trust_remote_code
datasets>=2.18.0          # HuggingFace datasets (Svarah)
jiwer>=3.0.0              # WER + CER computation
tqdm>=4.66.0              # progress bar
soundfile>=0.12.1         # audio backend for torchaudio